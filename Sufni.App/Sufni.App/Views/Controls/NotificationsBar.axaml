<UserControl
    IsVisible="{Binding Notifications.Count}"
    d:DesignHeight="50"
    d:DesignWidth="500"
    mc:Ignorable="d"
    x:Class="Sufni.App.Views.Controls.NotificationsBar"
    x:DataType="vm:ViewModelBase"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Sufni.App.ViewModels"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        Background="DarkKhaki"
        BorderBrush="Khaki"
        BorderThickness="2"
        CornerRadius="5"
        IsPointerOver="{Binding IsPointerOverNotifications, Mode=OneWayToSource}"
        MaxHeight="100"
        Padding="5">
        <Grid ColumnDefinitions="*" RowDefinitions="auto *">
            <Button
                Background="Khaki"
                Command="{Binding ClearNotificationsCommand}"
                Content="close"
                CornerRadius="2"
                Foreground="#0d0d0d"
                Grid.Row="0"
                Height="20"
                HorizontalAlignment="Right"
                HorizontalContentAlignment="Center"
                Padding="0"
                VerticalAlignment="Top"
                VerticalContentAlignment="Center"
                Width="50" />
            <ListBox
                Background="Transparent"
                Grid.Row="1"
                ItemsSource="{Binding Notifications}">
                <ListBox.Styles>
                    <Style Selector="ListBoxItem">
                        <Setter Property="Padding" Value="0,2, 0, 0" />
                    </Style>
                    <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
                        <Setter Property="Background" Value="Transparent" />
                    </Style>
                    <Style Selector="ListBoxItem:pointerover /template/ ContentPresenter">
                        <Setter Property="Background" Value="#19000000" />
                    </Style>
                </ListBox.Styles>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock
                            Foreground="#0d0d0d"
                            Text="{Binding}"
                            TextWrapping="Wrap" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
