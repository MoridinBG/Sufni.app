<Application
    x:Class="Sufni.App.App"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:labs="using:Avalonia.Labs.Controls"
    xmlns:theming="using:Sufni.App.Theming">
    <!--  Variant is set programmatically by ThemeBootstrap before XAML loads
          (persisted user preference). IThemeService manages runtime toggles.  -->

    <Application.Resources>
        <theming:SufniThemeResourceDictionary />
    </Application.Resources>

    <Application.Styles>
        <FluentTheme />
        <labs:ControlThemes />
        <StyleInclude Source="/Views/Plots/SufniPlotView.axaml" />
        <StyleInclude Source="/Views/Plots/VelocityBandView.axaml" />
        <StyleInclude Source="/Views/Plots/VibrationSummaryView.axaml" />
        <StyleInclude Source="avares://AvaloniaProgressRing/Styles/ProgressRing.xaml" />
        <StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml" />

        <!--  Global styles for TextBlock  -->
        <Style Selector="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SufniForeground}" />
        </Style>

        <!--  Global styles for Label  -->
        <Style Selector="Label">
            <Setter Property="Foreground" Value="{DynamicResource SufniFieldLabel}" />
        </Style>

        <!--  Global styles for Button  -->
        <Style Selector="Button:disabled Image">
            <Setter Property="Opacity" Value="{StaticResource SufniActionDisabledIconOpacity}" />
        </Style>

        <!--  Global styles for Buttons embedded in TextBoxes  -->
        <Style Selector="Button.embedded /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style Selector="Button.embedded:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Opacity" Value="0.8" />
        </Style>

        <!--  Global styles for "dangerous" Buttons  -->
        <Style Selector="Button.dangerous">
            <Setter Property="Foreground" Value="{DynamicResource SufniDangerColor}" />
        </Style>
        <Style Selector="Button.dangerous:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Foreground" Value="{DynamicResource SufniDangerColor}" />
        </Style>

        <!--  Global styles for "hyperlink" Buttons  -->
        <Style Selector="Button.hyperlink">
            <Setter Property="Foreground" Value="{DynamicResource SufniAccentHyperlink}" />
        </Style>
        <Style Selector="Button.hyperlink:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Foreground" Value="{DynamicResource SufniAccentHyperlink}" />
        </Style>

        <!--  Global styles for "item" Buttons  -->
        <Style Selector="Button.item">
            <Setter Property="Background" Value="{DynamicResource SufniListRowSurface}" />
            <Setter Property="RenderTransform" Value="{x:Null}" />
        </Style>
        <Style Selector="Button.item:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource SufniListRowSurfaceHover}" />
        </Style>
        <Style Selector="Button.item:pressed /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="{DynamicResource SufniListRowSurfaceHover}" />
        </Style>

        <!--  Global styles for NumericUpDown  -->
        <Style Selector="NumericUpDown">
            <Setter Property="Height" Value="{StaticResource SufniFieldHeight}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="{DynamicResource SufniFieldSurface}" />
            <Setter Property="Foreground" Value="{DynamicResource SufniFieldValue}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SufniFieldBorder}" />
        </Style>

        <!--  Global styles for TextBox  -->
        <Style Selector="TextBox">
            <Setter Property="MinHeight" Value="{StaticResource SufniFieldHeight}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <!--  Global styles for ComboBox  -->
        <Style Selector="ComboBox">
            <Setter Property="Height" Value="{StaticResource SufniFieldHeight}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="PlaceholderForeground" Value="{DynamicResource SufniFieldLabel}" />
        </Style>

        <!--  Global styles for CalendarDatePicker  -->
        <Style Selector="CalendarDatePicker Button">
            <Setter Property="FontSize" Value="{StaticResource SufniTypographyFieldTextFontSize}" />
        </Style>
        <Style Selector="CalendarDayButton">
            <Setter Property="FontSize" Value="{StaticResource SufniTypographyFieldTextFontSize}" />
        </Style>

        <!--  Global styles for CheckBox  -->
        <Style Selector="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource SufniForeground}" />
        </Style>
        <Style Selector="CheckBox:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Foreground" Value="{DynamicResource SufniForegroundPointerOver}" />
        </Style>

        <!--  Get rid of FlyoutPresenter's default MaxWidth  -->
        <Style Selector="FlyoutPresenter.wide">
            <Setter Property="MaxWidth" Value="10000" />
        </Style>

        <!--  Global styles for GridSplitter  -->
        <Style Selector="GridSplitter">
            <Setter Property="Background" Value="{DynamicResource SufniSplitterSurface}" />
            <Setter Property="MinHeight" Value="{StaticResource SufniSplitterMinThickness}" />
            <Setter Property="MinWidth" Value="{StaticResource SufniSplitterMinThickness}" />
        </Style>
    </Application.Styles>

</Application>
